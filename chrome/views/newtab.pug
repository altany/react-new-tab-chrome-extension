doctype html

html
    head
        meta(charset='UTF-8')
        title My Bookmarks
        style.
            #root {
                min-height: 100%
            }
    body
        #root
        if env !== 'prod'
        script(src=env == 'prod' ? '/js/todoapp.bundle.js' : 'http://localhost:3000/js/todoapp.bundle.js')
